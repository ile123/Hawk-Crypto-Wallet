export type LoginInfo = {
    email: string;
    password: string;
};

export type SendRegisterInfo = {
    name: string;
    password: string;
    email: string;
}

export type RegisterInfo = {
    name: string;
    password: string;
    repeatedPassword: string;
    email: string;
}